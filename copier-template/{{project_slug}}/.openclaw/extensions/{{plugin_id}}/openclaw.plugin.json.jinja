{
  "id": "{{ plugin_id }}",
  "name": "{{ plugin_name }}",
  "description": "{{ plugin_description }}",
  "version": "{{ version }}",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
